import { FC } from "react";
interface HeaderProps { }

export const Header: FC<HeaderProps> = () => {
    return (
        <header>
            <nav>
                <ul>
                </ul>
            </nav>
        </header>
    );
}
